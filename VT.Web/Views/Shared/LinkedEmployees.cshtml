@model VT.Services.DTOs.EmployeeSynchronizationList

<div id="modal-sync-employees-form" class="modal fade" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <form id="" action="@Url.Action("UpdateLinkedEmployee", "Quickbooks")"
              class="form-horizontal"
              method="POST" role="form">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">
                        <span aria-hidden="true">&times;</span><span class="sr-only">Close</span>
                    </button>
                    <h4 class="modal-title" style="text-align: center;" id="modal-sync-employees-title">
                        <b>Synchronized Employees</b>
                    </h4>
                    <small class="font-bold"></small>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-lg-12">
                            <div id="mainEmployeeDiv" style="text-align:center;">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>